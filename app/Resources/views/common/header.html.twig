<!-- 顶部 -->

    <div class="width100 float_l height80 line_hei80 header">
        <!-- 顶部左边 -->
        <div class="float_l">
            <div class="float_l margin_l20">
                <a href="{{ path('index') }}"><img src="{{ asset('img/logo.png')}}" width="150"></a>
            </div>
            <div class="float_l">
                <ul class="ul_li fon_siz16 nav-color">
                    <li><a>学院简介</a></li>
                    <li><a>项目特色</a></li>
                    <li><a>核心课程</a></li>
                    <li><a>学徒日志</a></li>
                    <li><a>学徒任务</a></li>
                    <li><a>互动社区</a></li>
                </ul>
            </div>
        </div>
        <!-- 顶部右边 -->
        <div class="float_r margin_r40">
            <div class="float_l top_input">
                <input class="posi_relative" type="text" name="" id="" placeholder="请输入想搜索的内容...">
                <a><i class="fa fa-search"></i></a>
            </div>
            {% if  is_granted('IS_AUTHENTICATED_FULLY') %}
            {% if  is_granted('ROLE_STUDENT')%}
                <div class="float_l margin_l20">
                    <a class="btn btn-outline" href="{{ path('profilo',{'username':app.user.username,'type':'assign'}) }}"><i class="fa fa-envelope"></i>
                        <span class="badge" style="font-size: 12px">
                                {{ app.session.get('assignments') }}
                        </span>
                    </a>
                </div>
            {% endif %}
                <div class="btn-group">
                    <a type="button" class="btn btn-outline dropdown-toggle" data-toggle="dropdown">
                        <img src="{{ asset('img/default_face.png')}}" width="25" height="25" style="border-radius: 100%">
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        {% if  is_granted('ROLE_STUDENT')%}
                            <li><a href="{{ path('profilo', {'username': app.user.username}) }}">个人中心</a></li>
                        {% endif %}
                        {% if  is_granted('ROLE_ADMIN') or is_granted('ROLE_TEACHER')%}
                            <li><a href="{{ path('admin_dashbroad') }}">管理后台</a></li>
                        {% endif %}
                        <li class="divider"></li>
                        <li><a href="{{ path('logout') }}">登出</a></li>
                    </ul>
                </div>
            {% else %}
                <div class="float_l margin_l30 margin_r40">
                    <button class="btn btn-outline" id="login" data-toggle="modal" data-target="#login_modal"><i class="fa fa-user"></i></button>
                </div>
            {% endif %}
        </div>
    </div>
<!-- 登录 -->
<div class="modal fade" id="login_modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" style="width:500px; ">
        <div class="modal-content">
            <div class="modal-body">
                {{ include('security/login.html.twig') }}
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal -->
</div>